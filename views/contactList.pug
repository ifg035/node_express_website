doctype html
html(lang="ko")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title 웹디자이너 임예은 포트폴리오
        include includes/comm_style.pug
    body 
        include includes/comm_header.pug
        h1(class="d-none") contact
        main 
            hgroup(class="text-center py-2")
                h2(class="caps mx2") CONTACT,REQUESTLIST:작업요청 및 문의
                h3 작업요청 목록
            article 
                h2(class="d-none") main - contactList
                div(class="container mx-auto")
                    table(class="contact_list")
                        tr 
                            th 번호
                            th 구분 
                            th 이름
                            th 이메일 
                            th 제목
                            th 파일 
                            th 내용 
                            th 등록일 
                    each data in dataset
                        tr
                            td= data.id
                            td= data.type
                            td= data.name
                            td= data.email
                            td= data.title
                            td= data.file
                            td= data.memo
                            td= data.regadate

        include includes/comm_footer.pug